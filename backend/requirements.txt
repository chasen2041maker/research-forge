# ===== AI Co-Scientist 后端依赖 =====
# 安装:pip install -r requirements.txt
# Python >= 3.11 推荐

# ---- 核心 Agent 框架 ----
langgraph>=0.2.50              # 主编排:状态图 + Checkpointer
langgraph-checkpoint-sqlite>=3.0.0  # SQLite Checkpointer 持久化
langchain-core>=0.3.0          # LangChain 基础类型
langchain-community>=0.3.0     # 工具集成(可选)
pyautogen>=0.4.0               # 多 Agent 圆桌(模块 4 预留,当前未引入)

# ---- MCP(Model Context Protocol,2024.11 Anthropic 发布)----
# 用途:把 m2 检索源独立成 MCP Server,供任何 MCP 客户端(Claude Desktop /
# Cursor / 其他团队 Agent)复用。可通过 settings.USE_MCP=true 开启。
# 详见 教学资料/11-MCP与外部集成/README.md
mcp>=1.0.0

# ---- LLM SDK(只用这两家) ----
openai>=1.50.0                 # GPT 中转站走 OpenAI 兼容协议
anthropic>=0.40.0              # Claude Opus 4.7

# ---- 文献检索 API(模块 2) ----
arxiv>=2.1.0                   # arXiv 官方包装
httpx>=0.27.0                  # 异步 HTTP(Semantic Scholar / OpenAlex)
aiohttp>=3.10.0                # 备用异步 HTTP
tenacity>=9.0.0                # 重试(API 限流时指数退避)

# ---- 知识图谱(模块 3) ----
networkx>=3.3                  # 轻量图(默认)
neo4j>=5.20.0                  # Neo4j 客户端(可选,完整模式)

# ---- 向量检索 ----
qdrant-client>=1.11.0          # Qdrant Python 客户端

# ---- 状态/缓存 ----
sqlalchemy>=2.0.30             # ORM
alembic>=1.13.0                # PostgreSQL schema migrations
psycopg2-binary>=2.9.9         # Postgres 驱动
redis>=5.0.0                   # 缓存
diskcache>=5.6.3               # 本地磁盘缓存兜底

# ---- 论文写作(模块 7) ----
pylatex>=1.4.2                 # LaTeX 生成
bibtexparser>=1.4.1            # BibTeX 解析

# ---- 代码沙箱(模块 6) ----
docker>=7.1.0                  # Docker SDK,执行 LLM 生成代码

# ---- Web 后端 ----
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=13.0               # 流式输出
celery>=5.4.0                  # 异步长任务
pydantic>=2.8.0
pydantic-settings>=2.4.0       # .env 配置加载
jsonschema>=4.23.0             # ReproductionSpec v1 contract validation

# ---- 工具/辅助 ----
python-dotenv>=1.0.1
loguru>=0.7.2                  # 比 logging 好用的日志
rich>=13.7.0                   # 漂亮的 CLI 输出
typer>=0.12.0                  # CLI 框架
tqdm>=4.66.0                   # 进度条

# ---- 测试 ----
pytest>=8.3.0
pytest-asyncio>=0.24.0
respx>=0.21.0                  # httpx mock

# ---- 开发工具(可选) ----
ruff>=0.6.0                    # lint + format
mypy>=1.11.0
