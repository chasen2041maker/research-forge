export const metadata = {
  title: "Research Studio · Research Forge",
  description: "Explore research directions before they enter Forge's evidence gate.",
};

export default function StudioLayout({ children }: { children: React.ReactNode }) {
  return children;
}
